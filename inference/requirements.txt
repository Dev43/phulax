fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Phase 2: transformers serving of the merged Qwen2.5-0.5B + LoRA. Activated
# at runtime when PHULAX_MODEL_DIR is set; otherwise the server stays in stub
# mode and these deps are unused (still installed so the image is uniform).
transformers==4.46.3
accelerate==1.1.1
safetensors>=0.4.5
torch==2.4.1 ; sys_platform != "darwin"
torch==2.4.1 ; sys_platform == "darwin"
sentencepiece>=0.2.0

# Phase 3: llama.cpp Q4 serving (3-5x faster than transformers FP16 on CPU).
# Pinned to a version with prebuilt linux/amd64 + arm64 Python 3.11 wheels so
# the inference image build doesn't fall back to a 5-minute source compile.
llama-cpp-python==0.3.2
jinja2>=3.1.4
